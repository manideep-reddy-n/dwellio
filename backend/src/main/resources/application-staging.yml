spring:
  config:
    activate:
      on-profile: staging
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  flyway:
    clean-disabled: true

dwellio:
  dev:
    seed-demo-data: false
  jwt:
    secret: ${JWT_SECRET}
